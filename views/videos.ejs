<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      * {
        box-sizing: border-box;
      }
    </style>
  </head>

  <body>
    <!-- createe a comdition to show video exit-->
    <div class="container">
      <% allvideos.forEach((order,index)=>{%>
      <video controls width="500">
        <source src="<%= order.imageUrl %>" type="video/mp4" />
        Your browser does not support the video tag.
      </video>
      <div class="video">
        <p><%= index+1 %></p>
        <h5><%= order.imageName %></h5>
        <button>
          <a href="/rec/<%=order.cloudinary_id %>">hello</a>
        </button>
      </div>
      <% })%>
    </div>
  </body>
</html>
